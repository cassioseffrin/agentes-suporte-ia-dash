# Rules for AI Agent

- **Nunca executar `git commit` ou `git push` automaticamente**: Quaisquer alterações de código devem apenas modificar/salvar os arquivos. Os comandos de `git commit` e `git push` devem ser executados **exclusivamente pelo usuário de forma manual**, a não ser que uma instrução direta da conversa peça explicitamente o commit/push.
